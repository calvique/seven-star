import React,{useState} from 'react';
import { SiteSeo } from '../../components/seo/SiteSeo';
import { ArrowLeft, Mail, MapPin, Phone, Send, CheckCircle } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Button, Card, Input, Select, Textarea } from '../../components/ui';
import { api } from '../../services/api';
import { useSettings } from '../../context/SettingsContext';
export function Contact(){const {getSettingValue}=useSettings();const name=getSettingValue('general','school.name','Seven Star English Boarding School');const address=getSettingValue('general','school.address','Devdaha-2, Rupandehi, Nepal');const phone=getSettingValue('general','school.phone','');const email=getSettingValue('general','school.email','');const facebook=getSettingValue('social','social.facebook','https://www.facebook.com/sevenstar.boarding');const[form,setForm]=useState({name:'',email:'',phone:'',subject:'',category:'general',message:''});const[done,setDone]=useState(false);const[error,setError]=useState('');const[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{await api.submitContact(form);setDone(true)}catch(err:any){setError(err?.response?.data?.message||'Unable to send message.')}finally{setLoading(false)}};return <><SiteSeo title="Contact" description={`Contact ${name}.`} /><div className="min-h-screen bg-gray-50"><section className="bg-primary-700 text-white py-16"><div className="container-custom"><Link to="/" className="inline-flex items-center gap-2 text-primary-100 mb-6"><ArrowLeft className="w-4 h-4"/> Back to Home</Link><h1 className="font-heading text-4xl font-bold">Contact Us</h1></div></section><section className="section"><div className="container-custom grid lg:grid-cols-3 gap-8"><Card className="lg:col-span-2"><h2 className="font-heading text-2xl font-bold">Send a message</h2>{done?<div className="py-12 text-center"><CheckCircle className="w-14 h-14 mx-auto text-green-600 mb-4"/><h3 className="text-xl font-semibold">Message sent</h3><p className="text-gray-600 mt-2">Thank you. The school can respond through the contact details you supplied.</p></div>:<form onSubmit={submit} className="mt-6 grid md:grid-cols-2 gap-5">{error&&<div className="md:col-span-2 p-3 bg-red-50 text-red-700 rounded-lg">{error}</div>}<Input label="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><Input label="Email" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><Input label="Phone" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><Select label="Category" value={form.category} onChange={e=>setForm({...form,category:e.target.value})} options={[{value:'general',label:'General'},{value:'admission',label:'Admission'},{value:'academic',label:'Academic'},{value:'facility',label:'Facility'},{value:'feedback',label:'Feedback'},{value:'complaint',label:'Complaint'}]}/><Input label="Subject" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})} required/><div className="md:col-span-2"><Textarea label="Message" value={form.message} onChange={e=>setForm({...form,message:e.target.value})} rows={6} required/></div><div className="md:col-span-2"><Button type="submit" loading={loading}><Send className="w-4 h-4 mr-2"/> Send message</Button></div></form>}</Card><div className="space-y-5"><Card><h2 className="font-heading text-xl font-semibold">School contact</h2><div className="mt-5 space-y-4 text-gray-600"><div className="flex gap-3"><MapPin className="w-5 h-5 text-primary-600 flex-shrink-0"/><span>{address}</span></div>{phone&&<div className="flex gap-3"><Phone className="w-5 h-5 text-primary-600 flex-shrink-0"/><a href={`tel:${phone}`}>{phone}</a></div>}{email&&<div className="flex gap-3"><Mail className="w-5 h-5 text-primary-600 flex-shrink-0"/><a href={`mailto:${email}`}>{email}</a></div>}</div></Card><Card><h2 className="font-heading text-xl font-semibold">Social</h2><a href={facebook} target="_blank" rel="noreferrer" className="inline-flex mt-4 text-primary-600 hover:underline">Facebook</a></Card></div></div></section></div></>}
