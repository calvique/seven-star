import { useState } from 'react';
import { Link } from 'react-router-dom';
import { Mail } from 'lucide-react';
import { Button, Card, Input } from '../../components/ui';
import { useAuth } from '../../context/AuthContext';
import { SiteSeo } from '../../components/seo/SiteSeo';
import { useSettings } from '../../context/SettingsContext';
export function ForgotPassword(){const{forgotPassword}=useAuth();const{getSettingValue}=useSettings();const name=getSettingValue('general','school.name','Seven Star English Boarding School');const[email,setEmail]=useState('');const[done,setDone]=useState(false);const[error,setError]=useState('');const[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');setLoading(true);try{await forgotPassword(email);setDone(true)}catch(err:any){setError(err?.response?.data?.message||'Unable to process request.')}finally{setLoading(false)}};return <><SiteSeo title="Forgot Password" description={`Reset your ${name} portal password.`}/><div className="min-h-[70vh] flex items-center justify-center py-12"><Card className="w-full max-w-md">{done?<div className="text-center py-8"><Mail className="w-12 h-12 mx-auto text-green-600"/><h1 className="font-heading text-2xl font-bold mt-4">Check your email</h1><p className="text-gray-600 mt-2">If the account exists, a password reset link has been requested.</p><Link to="/login" className="inline-flex mt-6 text-primary-600 hover:underline">Back to login</Link></div>:<><h1 className="font-heading text-2xl font-bold">Forgot password?</h1><p className="text-gray-500 mt-1">Enter your email to request a reset link.</p>{error&&<div className="mt-4 p-3 rounded-lg bg-red-50 text-red-700 text-sm">{error}</div>}<form onSubmit={submit} className="mt-6 space-y-4"><Input label="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><Button type="submit" loading={loading} className="w-full">Send reset link</Button></form><Link to="/login" className="block text-center mt-5 text-primary-600 hover:underline">Back to login</Link></>}</Card></div></>}
