import React,{useState} from 'react';
import { Helmet } from 'react-helmet-async';
import { Link } from 'react-router-dom';
import { Button, Input } from '../../components/ui';
import { useAuth } from '../../context/AuthContext';
import { useSettings } from '../../context/SettingsContext';
export function ForgotPassword(){const {forgotPassword}=useAuth();const {getSettingValue}=useSettings();const schoolName=getSettingValue('general','school.name','Seven Star English Boarding School');const[email,setEmail]=useState('');const[done,setDone]=useState(false);const[error,setError]=useState('');const[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{await forgotPassword(email);setDone(true)}catch(err:any){setError(err?.response?.data?.message||'Unable to send reset request.')}finally{setLoading(false)}};return <><Helmet><title>Forgot Password | {schoolName}</title></Helmet><div><h2 className="font-heading text-2xl font-bold">Reset your password</h2><p className="text-gray-500 mt-1">Enter your email address.</p>{done?<div className="mt-6 p-4 bg-green-50 text-green-700 rounded-lg">If the account exists, a password reset email has been requested.</div>:<form onSubmit={submit} className="mt-6 space-y-5">{error&&<div className="p-3 bg-red-50 text-red-700 rounded-lg text-sm">{error}</div>}<Input label="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><Button type="submit" loading={loading} className="w-full">Send reset link</Button></form>}<Link to="/login" className="block text-center mt-5 text-primary-600 hover:underline">Back to login</Link></div></>}
